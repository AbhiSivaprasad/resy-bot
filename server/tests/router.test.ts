import nock from 'nock';
import { RESY_API_URL } from '../src/external/api';

// write a test for the router
describe('testing /user', () => {
    test('user exists', () => {
        //        nock(RESY_API_URL)
        //            .persist()
        //            .get('/')
        //            .reply(function () {
        //                return [200, this.responseBody];
        //            });
        //        expect(true).toBe(true);
    });
});
